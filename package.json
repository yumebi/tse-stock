{
  "name": "tse-stock",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "push:release": "node bump-version.mjs && git add src-tauri/tauri.conf.json src-tauri/Cargo.toml && git commit -m \"chore: バージョンアップ\" && git push origin master"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.0",
    "vite": "^6.0.0"
  }
}
